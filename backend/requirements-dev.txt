# VMotors · dependencias solo para correr las pruebas (backend/tests/)
# Instalacion:  pip install -r requirements.txt -r requirements-dev.txt
# Ejecucion:    pytest

-r requirements.txt
pytest>=7.4
httpx>=0.27      # lo usa fastapi.testclient.TestClient
