# VMotors · dependencias del backend
# Instalacion:  pip install -r requirements.txt

fastapi>=0.111
uvicorn[standard]>=0.29
itsdangerous>=2.2        # requerido por SessionMiddleware (cookies de sesion firmadas)
python-dotenv>=1.0
email-validator>=2.1     # requerido por pydantic.EmailStr
